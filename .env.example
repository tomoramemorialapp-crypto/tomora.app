# Tomora — Supabase configuration
# These are EXPO_PUBLIC_ vars, so they are embedded in the client bundle.
# Use the PUBLISHABLE key (sb_publishable_...), never the service_role/secret key.
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx

# Public app URL for invite, claim, profile, and OAuth redirect links.
# Use http://localhost:8081 for local web development.
# Add http://localhost:8081/auth/callback and https://tomora.app/auth/callback
# to Supabase Auth → URL configuration when enabling Google/Apple sign-in.
EXPO_PUBLIC_APP_URL=https://tomora.app
