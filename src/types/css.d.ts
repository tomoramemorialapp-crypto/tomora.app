/**
 * CSS module declarations for TypeScript.
 * Expo normally generates these in gitignored `expo-env.d.ts` after `expo start`;
 * we commit this so `tsc --noEmit` passes in CI without a prior Expo run.
 */
declare module '*.css';

declare module '@/global.css';
