# Tomora — Supabase configuration
# These are EXPO_PUBLIC_ vars, so they are embedded in the client bundle.
# Use the PUBLISHABLE key (sb_publishable_...), never the service_role/secret key.
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx

# Public app URL for invite, claim, profile, OAuth, and email-verification redirects.
# Web sign-up uses window.location.origin when available; native uses this value.
#
# Supabase Auth → URL configuration:
#   Site URL: https://tomora.app
#   Redirect URLs (add all that apply):
#     https://tomora.app/**
#     https://tomora.app/auth/callback
#     http://localhost:8081/auth/callback
#   The wildcard is required so query params (next=reset-password, etc.) are accepted.
#
# OAuth (Google / Apple) — optional; buttons stay hidden until enabled.
# When ready:
#   1. Supabase → Authentication → Providers → enable Google and/or Apple
#   2. Add the redirect URLs above to each provider if prompted
#   3. Set EXPO_PUBLIC_OAUTH_SIGN_IN_ENABLED=true and rebuild
# EXPO_PUBLIC_OAUTH_SIGN_IN_ENABLED=false
EXPO_PUBLIC_APP_URL=https://tomora.app
